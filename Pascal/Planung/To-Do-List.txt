DONE:
    Docker
        für Grafana

    Python
        -write
        -query

    SQL / Flux:
        CREATE / existiert nicht
        DELETE / existiert nicht
        SELECT / FROM, FILTER, KEEP, YIELD
        GROUP BY / GROUP

TO-DO:
    Python
        -plot with pandas
            - Warum werden die Labels \/ Achsenbeschriftungen nicht angezeit? Warum ist der Titel "Figure 1"? Fix That PLS.
        -delete data older than 15min

    SQL / Flux:
        DROP / DROP
        DATA MANIPULATION: PIVOT, MEAN
    
    Dokumentation
        -halt schreiben

WON'T BE DONE:
    Docker
        mit Python (WON'T BE DONE)

    Python
        -alles mit jupyter notebook
    
    Grafana
        -flux queries