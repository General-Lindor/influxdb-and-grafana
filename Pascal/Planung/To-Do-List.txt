To-Do-Liste
    Docker
        für Grafana (DONE)
        mit Python
    
    Python
        -write
            - Auswahl der Daten, die gespeichert werden sollen
        -query (DONE)
        -plot with pandas
            - Warum werden die Labels \/ Achsenbeschriftungen nicht angezeit? Warum ist der Titel "Figure 1"? Fix That PLS.
        -delete data older than 15min
        -alles mit jupyter notebook
    
    Grafana
        -flux queries

    SQL / Flux:
        CREATE / existiert nicht (DONE)
        DROP / DROP
        DELETE / existiert nicht (DONE)
        SELECT / FROM, FILTER, KEEP, YIELD (DONE)
        DATA MANIPULATION: PIVOT, MEAN
        GROUP BY / GROUP (DONE)
    
    Dokumentation
        -halt schreiben